services:
  api:
    build:
      context: .
      dockerfile: DockerFile
    environment:
      ASPNETCORE_URLS: http://+:8080
    env_file:
      - .env
    ports:
      - "8082:8080"
    volumes:
      - ./uploads_data:/app/wwwroot/uploads
      - ./logs_data:/app/logs
    networks:
      - web
      - sail
    labels:
      - traefik.enable=true
      - traefik.docker.network=web
      - traefik.http.routers.back-matos.rule=Host(`matos.pierreleroyer.fr`) && PathPrefix(`/api`)
      - traefik.http.routers.back-matos.entrypoints=websecure
      - traefik.http.routers.back-matos.tls=true
      - traefik.http.routers.back-matos.tls.certresolver=tlsresolver
      - traefik.http.routers.back-matos.priority=2
      - traefik.http.services.back-matos.loadbalancer.server.port=8082
  pgsql:
    image: postgres:17-alpine
    ports:
      - ${FORWARD_DB_PORT:-5432}:5432
    environment:
      PGPASSWORD: ${DB_PASSWORD:-secret}
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
    volumes:
      - sail-pgsql:/var/lib/postgresql/data
    networks:
      - sail
    healthcheck:
      test:
        - CMD
        - pg_isready
        - -q
        - -d
        - ${DB_DATABASE}
        - -U
        - ${DB_USERNAME}
      retries: 3
      timeout: 5s

volumes:
  sail-pgsql:
    external: true
    name: back_sail-pgsql
networks:
  web:
    external: true
  sail:
    driver: bridge
